<% @items.each do |item| %>
    <% post = Post.find_by(id: item.post_id) %>
    <%= "注文商品：#{post.title}" %>
    <%= "金額：#{post.price}円" %>
<% end %>
<%= "名前：#{@guest.lastname}#{@guest.firstname}" %>
<%= "住所：#{@guest.prefectures}#{@guest.municipalities}#{@guest.address}" %>
<%= "郵便番号：#{@guest.first_zip_code}#{@guest.last_zip_code}" %>
<%= "電話番号：#{@guest.phone_number}" %>
<%= "メール：#{@guest.email}" %>
<%= "予測注文時間：#{@guest.created_at}" %>